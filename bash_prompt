#!/usr/bin/env bash

PROMPT_DIR="./bash_prompt.d"

for f in "$PROMPT_DIR"/*.sh; do
  [ -r "$f" ] && source "$f"
done

SEGMENTS=(
  "path|📁|render_path|none:gray:yellow:gray"
  "git|🌿|render_git|none:green+bold:gray:bright_green?red:gray"
  "user|👤|render_user|blue"
  "host|🖥️|render_host|orange"
  "time|🕐|render_time|white"
)

prompt_init SEGMENTS "path git" "time" "user host" "orange"
