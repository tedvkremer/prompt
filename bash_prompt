#!/usr/bin/env bash
# Bash prompt with top-fixed status bar

PROMPT_DIR="./bash_prompt.d"

for f in "$PROMPT_DIR"/*.sh; do
  [ -r "$f" ] && source "$f"
done

SEGMENTS=(
  "path|📁:2|render_path|green+bold"
  "git|⎇:2|render_git|gray:blue+bold:purple:red?green:gray"
  "user|👤:2|render_user|blue+bold"
  "host|🖥️:2|render_host|orange+bold"
  "time|🕐:2|render_time|gray"
)

prompt_init SEGMENTS "path git" "time" "user host" "orange"
