#!/usr/bin/env bash

PROMPT_DIR="./bash_prompt.d"

for f in "$PROMPT_DIR"/*.sh; do
  [ -r "$f" ] && source "$f"
done

SEGMENTS=(
  "path|📁|render_path|green"
  "git|🌿|render_git|gray:none:purple:green?red:gray"
  "user|👤|render_user|blue"
  "host|🖥️:2|render_host|orange"
  "time|🕐|render_time|gray"
)

prompt_init SEGMENTS "path git" "time" "user host" "orange"
